apiVersion: v1
kind: Service
metadata:
  name: ouroboros-orchestrator
  namespace: ouroboros
spec:
  selector:
    app: ouroboros-orchestrator
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP

